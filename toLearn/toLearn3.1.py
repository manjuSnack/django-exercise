#3.1 Migrations

# Run Server
# Terminal -- poetry shell 
# : poetry에 통해 django project로 접속

# manage.py
# : Server를 실행하는 등 설정하는 파일

# Terminal -- python manage.py runserver
# : Terminal에서 실행 시 django server를 실행시킬 수 있다.

# db.sqlite3
# : django server 실행 시 생성하는 데이터베이스 파일이며 데이터베이스의 테이블이 작성되는 파일이다.

# localhost/admin/
# : django server의 admin에 접근할 수 있다. 

# migrations
# : database의 state를 수정하는 것을 일컫는다.

# Terminal -- python manage.py migrate
# : 필요한 migration을 db.sqlite3에 추가한다.
